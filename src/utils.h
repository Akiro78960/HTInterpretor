#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

void printVector( std::vector<std::string> v);
void vectorToFile(std::vector<std::string> v, std::ofstream f);
